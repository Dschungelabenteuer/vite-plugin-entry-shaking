/**
 * > and then exporting "Foo" from a target file
 *
 *  This is the target file as set up through the plugin's
 * `targets` option in the `vite.config.ts` file.
 *
 */
export { Foo } from './Original';
