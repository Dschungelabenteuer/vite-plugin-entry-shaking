/**
 * > and then re-exporting "Bar" from a target file
 *
 *  This is the arget file as set up through the plugin's
 * `targets` option in the `vite.config.ts` file.
 *
 * So let's re-export Bar.
 */
export { Bar } from './Bar';
