export type RouterButtonProps = {
  /** Router button label. */
  label: string;
  /** Router button Icon. */
  icon?: string;
  /** Router button route. */
  href: string;
  /** Router button count. */
  count?: number;
};
