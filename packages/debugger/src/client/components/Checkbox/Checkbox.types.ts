export type CheckboxProps = {
  /** Input ID. */
  id: string;
  /** Option label. */
  label: string;
  /** Model value. */
  modelValue: boolean;
  /** Is the option disabled? */
  disabled?: boolean;
};
